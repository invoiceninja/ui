import React from 'react';

// Stub component to fix build error
export function DashboardRowContainer(props: any) {
  return (
    <div>
      <p>DashboardRowContainer - This is a placeholder component</p>
      {props.children}
    </div>
  );
}
