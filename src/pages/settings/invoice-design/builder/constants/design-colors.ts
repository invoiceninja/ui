/**
 * Default CSS colors for invoice designer blocks (canvas + HTML export).
 * Value columns and monetary amounts default to true black; labels stay muted gray.
 */

export const DEFAULT_VALUE_TEXT_COLOR = '#000000';

export const DEFAULT_LABEL_TEXT_COLOR = '#6B7280';
